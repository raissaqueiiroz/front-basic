import { responseApi } from "./fetchApiResponse.js";

document.addEventListener("DOMContentLoaded", async function () {
  // Código que será executado quando o navegador carregar
  await responseApi();
});
